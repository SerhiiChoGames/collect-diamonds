name: collect-diamonds-game

services:
    app:
        image: collect-diamonds-game
        container_name: collect-diamonds-game
        build:
            context: .
            dockerfile: Containerfile
        volumes:
            - .:/app:z
            - /app/node_modules
        ports:
            - 3000:5173
